<Window x:Class="WinQuickLook.QuickLookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        SizeToContent="WidthAndHeight" WindowStyle="None" WindowStartupLocation="CenterScreen" Topmost="True" ShowActivated="True" ShowInTaskbar="False">
    <Border SnapsToDevicePixels="True">
        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="5" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Text="{Binding Title}" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0"/>
            <Button x:Name="open" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center">関連付けられたアプリで開く</Button>
            <Border Background="White" Grid.Row="2" MinWidth="450" MinHeight="300">
                <ContentPresenter Content="{Binding PreviewHost}" />
            </Border>
        </Grid>
    </Border>
</Window>
