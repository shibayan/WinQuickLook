<UserControl x:Class="WinQuickLook.Controls.MusicFileControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:WinQuickLook.Converters"
             mc:Ignorable="d"
             d:DesignWidth="700" d:DesignHeight="400">
    <UserControl.Resources>
        <converters:FileSystemInfoToThumbnailConverter x:Key="FileSystemInfoToThumbnailConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.4*" />
            <ColumnDefinition Width="0.6*" />
        </Grid.ColumnDefinitions>
        <Image Source="{Binding FileInfo, Converter={StaticResource FileSystemInfoToThumbnailConverter}}" Stretch="UniformToFill" Opacity="0.5" Grid.ColumnSpan="2">
            <Image.Effect>
                <BlurEffect Radius="200" />
            </Image.Effect>
        </Image>
        <Image Source="{Binding FileInfo, Converter={StaticResource FileSystemInfoToThumbnailConverter}}" Margin="25,15,15,15" MaxWidth="256" MaxHeight="256" Stretch="Uniform" Grid.Column="0" />
        <StackPanel Orientation="Vertical" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,15,15,15">
            <TextBlock x:Name="title" FontSize="24" TextWrapping="Wrap" TextAlignment="Center" />
            <TextBlock x:Name="artist" FontSize="16" TextWrapping="Wrap" Margin="0,15,0,0" TextAlignment="Center" />
            <TextBlock x:Name="album" FontSize="16" TextWrapping="Wrap" Margin="0,5,0,0" TextAlignment="Center" />
        </StackPanel>
        <MediaElement x:Name="mediaElement" Source="{Binding FileInfo.FullName}" LoadedBehavior="Manual" Grid.Column="0" />
    </Grid>
</UserControl>
